"use client";

/**
 * PLACEHOLDER — Act I: scenes 1–3 (the lawyer, the gown + IP elements, the gavel). progress 0.00 → ~0.44
 * Replace with the real implementation. Must be a pure R3F subtree (no DOM), reading the story clock via useStoryFrame.
 */
export function FigureAct() {
  return null;
}
