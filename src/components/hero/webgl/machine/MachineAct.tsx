"use client";

/**
 * PLACEHOLDER — Act II: scenes 4–8 (gears/patent, design, trade mark, GI map, legal world). progress ~0.40 → 1.00
 * Replace with the real implementation. Must be a pure R3F subtree (no DOM), reading the story clock via useStoryFrame.
 */
export function MachineAct() {
  return null;
}
